#ifndef ADD_H
#define ADD_H

// We need to do also the forward declarations inside the namespace
namespace BasicMath
{
    // function add() is part of namespace BasicMath
    int add(int x, int y);
}

#endif
